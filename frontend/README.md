# 小米商城智能导购 Agent 前端

前端工程采用 React + TypeScript + Vite + Tailwind CSS。

## Scripts

```bash
npm install
npm run dev
npm run build
npm run test
```

开发环境默认通过 Vite proxy 将 `/api` 转发到 `http://localhost:8080`。
